# URL of the ClaimLens API. The Next.js server forwards every /api/* request to it (see next.config.ts),
# so the browser never calls the API directly. No trailing slash.
# Local development: http://localhost:8000   Vercel: your Render URL, e.g. https://claimlens-api.onrender.com
BACKEND_URL=http://localhost:8000
